import type { Certification } from "@/types";

/**
 * Certifications Content Collection
 * Strongly typed array of verified credentials.
 */
export const certifications: ReadonlyArray<Certification> = [];
