/**
 * Purpose: Central barrel export for all shared components (layout, shell, global overlays).
 * Used By: Global pages, root layout, and feature views.
 * Accessibility: Re-exports accessible, landmark-structured shared components.
 * Notes: Entry point for shared component imports (@/components/shared).
 */

export * from "./layout";
export * from "./navigation";

